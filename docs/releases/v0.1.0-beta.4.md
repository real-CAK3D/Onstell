# Onstell v0.1.0-beta.4 Release Checklist

This checklist prepares the next development beta. It should not be published as a user-ready KVM release.

## Release Positioning

Beta.4 is for smoke testing the desktop shell after Milestone 2 planning and simulation work.

Clearly state:

- Onstell is still not a working KVM.
- Routing is modeled and simulated only.
- Input capture is not active by default and has only a development-only guard.
- The loopback follower is in-process and fake.
- Pairing/session transport is designed but not implemented.
- Clipboard sync remains design-only.
- No real keyboard, pointer, clipboard, LAN control, or file-transfer traffic is sent.

Keep [v0.1.0-beta.3](https://github.com/real-CAK3D/Onstell/releases/tag/v0.1.0-beta.3) available as the first clean desktop-shell smoke-test release.

## Included Since beta.3

- Routing and permission readiness panel.
- Pure TypeScript routing state machine.
- Release Input action in the widget and tray.
- Read-only input capture spike plan and fail-closed development guard.
- Local in-process loopback follower simulator.
- Trusted LAN pairing/session transport design.
- Wiki pages for the Milestone 2 safety model.

## Still Simulated or Design-Only

| Area | beta.4 state |
| --- | --- |
| Keyboard/pointer capture | Not implemented. Development guard only. |
| Input suppression | Not implemented. |
| Remote input injection | Not implemented. |
| Routing | Pure state model plus fake loopback simulator. |
| Emergency release | UI/tray command stub and simulated release-state clearing. |
| Pairing | Placeholder trust states only. |
| LAN transport | Design doc only. |
| Clipboard sync | Design-only status, no clipboard reads or writes. |
| File transfer | Not implemented. |
| Signing/notarization | Not production-ready. |

## Pre-Release Checks

- [ ] `npm ci`
- [ ] `npm.cmd run check`
- [ ] `npm.cmd run build`
- [ ] GitHub Actions `CI` is green on the release commit.
- [ ] GitHub Actions `Security` is green on the release commit.
- [ ] Release workflow completes for Windows, Linux x64, and macOS.
- [ ] `SHA256SUMS.txt` is attached to the draft release.
- [ ] `RELEASE-NOTES.md` is attached to the draft release.
- [ ] Native package assets are attached, or the release notes explicitly say which platform artifact is missing.
- [ ] Downloaded asset checksums match `SHA256SUMS.txt`.
- [ ] Draft release notes clearly say beta.4 is not a working KVM.
- [ ] No failed or duplicate beta.4 draft releases remain on the Releases page.

## Draft Release Notes

```markdown
# Onstell v0.1.0-beta.4

This is a development beta for smoke testing the Onstell desktop shell and Milestone 2 safety surfaces.

What is included:

- Glass floating widget shell.
- Tray/menu presence.
- Local device and monitor layout prototype.
- Routing and permission readiness panel.
- Pure routing state machine for trust-gated state transitions.
- Release Input action in the widget and tray.
- Read-only input capture spike guard, disabled by default.
- In-process fake loopback follower simulator.
- Trusted LAN pairing/session transport design docs.

What is still simulated or design-only:

- Routing is simulated only.
- Input capture is not implemented for normal users.
- The loopback follower does not use real keyboard, pointer, clipboard, or network APIs.
- Pairing and encrypted LAN sessions are not implemented.
- Clipboard sync is not implemented.
- File transfer is not implemented.

This is not a working KVM release yet. Use beta.4 only to test the desktop shell, packaging, widget states, release command, and local simulation surfaces.
```

## Failed Draft Cleanup

If a beta.4 release workflow creates a bad draft:

1. Do not publish it.
2. Download any useful logs from the failed workflow.
3. Delete the failed draft release from GitHub Releases.
4. Delete any accidentally created duplicate `v0.1.0-beta.4` tag only if it points to the wrong commit.
5. Re-run the release workflow from the intended commit.

Do not delete beta.3 while preparing beta.4.
